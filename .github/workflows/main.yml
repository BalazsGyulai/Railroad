name: build and publish

on:
  push:
    branches: [develop]
  pull_request:
    branches: [develop]

jobs:
  build:
    runs-on: ubuntu-latest

  steps:
    - name: Install
    run: npm install

    - name: build
    run: npm run build

    - name: rename
    run: mv build docs
